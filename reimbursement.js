function reimbursement(){
 const r=trip?.reimbursement;
 if(!r){main.innerHTML=`<h2>報帳提醒</h2><section class="panel"><h3>加入你的報帳注意事項</h3><p>請匯入包含報帳資訊的新版私人行程檔。原有備忘與行李勾選會保留。</p><button class="btn primary" data-trip-import>匯入私人行程</button><input type="file" id="tripfile" accept="application/json,.json" hidden></section>`;return;}
 main.innerHTML=`<h2>報帳提醒</h2><div class="notice"><strong>登機前，先截圖保存電子登機證</strong>紙本請妥善保存；遺失須自費向航空公司補件。</div><section class="panel"><h3>開發票，出示這張卡</h3><p class="muted">發票須有公司抬頭與統編，否則無法報帳。</p>${r.company.map((v,i)=>`<div class="company-row"><div><small>${['公司抬頭','英文名稱','統一編號'][i]}</small><br><strong>${esc(v)}</strong></div><button class="btn" data-copy="${esc(v)}" aria-label="複製${['公司抬頭','英文名稱','統一編號'][i]}">複製</button></div>`).join('')}</section><h3>各類憑證怎麼留</h3><p class="muted">點開查看規則；英文標籤對應助理建立的資料夾。</p>${r.groups.map((g,i)=>`<details class="panel claim-group"><summary><span>${esc(g[0])}${g[1]?`<br><span class="pill">${esc(g[1])}</span>`:''}</span></summary><ul>${g[2].map(t=>`<li>${esc(t)}</li>`).join('')}</ul>${i===1?`<div class="actions">${link('https://www.tymetro.com.tw/tymetro-new/tw/_pages/service/record.html','機捷搭車證明申請')}</div>`:''}</details>`).join('')}<section class="panel"><h3>上傳報帳憑證</h3><p>將相關文件放到助理建立的報帳資料夾。這個 App 提供分類提醒，不會替你上傳文件。</p><p class="muted">網路費暫以 Others 作為建議分類；實際歸檔依助理指示。依你提供的補充通知整理。</p></section>`;
}
